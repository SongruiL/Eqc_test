# EQC Studio 本地密钥文件模板（前端 LLM Agent 用）。
#
# 用法：复制成 .eqc-secret（放在【启动 serve 的目录】，通常是 projects_2026 根，
# 即 eqc-workspace.yaml 旁边），填入你的真 key，保存。serve 启动会自动加载（不进 repo）。
# 也可改放别处，用 EQC_SECRET_FILE=路径 指定。真·环境变量优先于本文件。
#
#   cp eqc-secret.example  <projects_2026>/.eqc-secret
#   # 然后编辑 .eqc-secret 填 key
#
# 改完无需重建前端；重启 serve 即生效。

# 必填：Anthropic API key（sk-ant-... ）。绝不要把真 key 提交进任何仓库或贴进对话。
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxx

# 这台机器必填：直连 api.anthropic.com 被地理封锁，走本地代理（clash/系统代理）。
EQC_LLM_PROXY=http://127.0.0.1:10808

# 可选：一行切模型（缺省 claude-sonnet-4-6）。需要更强推理可换 claude-opus-4-8。
# EQC_LLM_MODEL=claude-sonnet-4-6
