% 高级运算符测试 (OPERATORS_ADVANCED)
% 自动生成，请勿手动编辑

% test_gamma: Gamma函数测试
% y_{gamma} = \Gamma(p1)

% test_loggamma: LogGamma函数测试
% y_{loggamma} = \ln\Gamma(p1)

% test_beta: Beta函数测试
% y_{beta} = \mathrm{B}(p1, p2)

% test_betainc: 不完全Beta函数测试
% y_{betainc} = I_{p3}(p1, p2)

% test_erf: 误差函数测试
% y_{erf} = \mathrm{erf}(p3)

% test_erfc: 补误差函数测试
% y_{erfc} = \mathrm{erfc}(p3)

% test_besselj: 第一类贝塞尔函数测试
% y_{besselj} = J_{p5}(p1)

% test_bessely: 第二类贝塞尔函数测试
% y_{bessely} = Y_{p5}(p1)

% test_besseli: 修正贝塞尔第一类测试
% y_{besseli} = I_{p5}(p1)

% test_besselk: 修正贝塞尔第二类测试
% y_{besselk} = K_{p5}(p1)

% test_digamma: Digamma函数测试
% y_{digamma} = \psi(p1)

% test_factorial: 阶乘测试
% y_{factorial} = p4!

% test_norm_cdf: 正态分布CDF测试
% y_{norm_cdf} = \Phi\left(\frac{0 - 0}{1}\right)

% test_norm_pdf: 正态分布PDF测试
% y_{norm_pdf} = \frac{1}{1\sqrt{2\pi}} e^{-\frac{(0 - 0)^2}{21^2}}

% test_chi2_cdf: 卡方分布CDF测试
% y_{chi2_cdf} = F_{\chi^2_{p4}}(p1)

% test_t_cdf: t分布CDF测试
% y_{t_cdf} = F_{t_{p4}}(1)

% test_poisson_pmf: 泊松分布PMF测试
% y_{poisson} = \frac{p1^{3} e^{-p1}}{3!}

% test_binomial: 组合数测试
% y_{binomial} = \binom{10}{3}

% test_complex: 复数构造测试
% y_{complex} = p1 + p2i

% test_complex_real: 复数实部测试
% y_{real} = \Re(p1 + p2i)

% test_complex_imag: 复数虚部测试
% y_{imag} = \Im(p1 + p2i)

% test_combined_special: 组合特殊函数测试
% y_{combined} = \Gamma(p1) + \mathrm{erf}(p3)
